server:
  port: 8081

spring:
  application:
    name: inventory-service

inventory:
  ranking:
    price-weight: 0.6
    duration-weight: 0.4

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/